version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: royal-grace-backend:local
    container_name: royal-grace-backend
    ports:
      - "127.0.0.1:8080:8080"  # Only accessible from localhost (for Nginx)
    environment:
      SPRING_DATASOURCE_URL: jdbc:h2:file:/data/royalgrace
      SPRING_DATASOURCE_USERNAME: sa
      SPRING_DATASOURCE_PASSWORD: ""
      LOGGING_LEVEL_ORG_HIBERNATE_SQL: warn
      ADMIN_DEFAULT_USERNAME: admin
      ADMIN_DEFAULT_PASSWORD: change-me
      ADMIN_DEFAULT_EMAIL: ""
    volumes:
      - h2data:/data
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    image: royal-grace-frontend:local
    container_name: royal-grace-frontend
    depends_on:
      - backend
    ports:
      - "127.0.0.1:3000:3000"  # Only accessible from localhost (for Nginx)
    environment:
      API_BASE_URL: http://backend:8080
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: change-me
      ADMIN_JWT_SECRET: super-secret-change-me
    restart: unless-stopped

volumes:
  h2data: